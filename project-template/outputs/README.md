# Outputs

Generated figures, tables and models. Written by the analysis documents in `reports/`, not edited by hand.

- `figures/` — plots
- `tables/` — exported tables
- `models/` — fitted model objects, ignored by git
