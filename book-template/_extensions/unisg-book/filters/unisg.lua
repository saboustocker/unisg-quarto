-- Placeholder filter for future automatic UniSG transformations.
-- Keep this intentionally small for now: it makes it possible to add
-- recurring manuscript/slide/site transformations later without changing
-- the public format names.

function Pandoc(doc)
  return doc
end
